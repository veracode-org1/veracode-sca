name: Sync Repo

on:
  pull_request:
    branches:
      - main
    types:
      - closed

jobs:
  sync_sca:
    if: ${{ github.event.pull_request.merged == true }}
    runs-on: ubuntu-latest
    name: Sync veracoe-sca repo
    steps:
      - name: Checkout veracode-sca repo
        uses: actions/checkout@v4
        with:
          path: veracode-sca

      - name: Checkout ghes-actions-integration repo
        uses: actions/checkout@v4
        with:
          repository: veracode-org1/ghes-actions-integration
          token: ${{ secrets.PAT_TOKEN }}
          path: ghes-actions-integrationin

      - name: Copy dist and action.yml files to ghes-actions-integration/actions folder
        run: |
          cp -r veracode-sca/dist ghes-actions-integrationin/actions/sca-scan/
          cp veracode-sca/action.yml ghes-actions-integrationin/actions/sca-scan/

      - name: Commit and push changes to ghes-actions-integration
        run: |
          cd ghes-actions-integrationin
          git config user.name "santhoshVC92"
          git config user.email "skesavan@veracode.com"
          git remote set-url origin https://x-access-token:${{ secrets.PAT_TOKEN }}@github.com/veracode-org1/ghes-actions-integration.git
          git add actions
          git commit -m "Sync Veracode SCA dist and yml"
          git push
